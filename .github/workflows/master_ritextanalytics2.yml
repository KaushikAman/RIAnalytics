# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - ritextanalytics2

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v1

    - name: Log in to registry
      uses: docker/login-action@v1
      with:
        registry: https://ritextanalytics2.azurecr.io/
        username: ${{ secrets.AzureAppService_ContainerUsername_e4d7d1e4cdf64e378295a6c19aa12faa }}
        password: ${{ secrets.AzureAppService_ContainerPassword_ac4b9bc85a40422d97a3a5f928e15b62 }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v2
      with:
        push: true
        tags: ritextanalytics2.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_e4d7d1e4cdf64e378295a6c19aa12faa }}/rianalytics:${{ github.sha }}
        file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'ritextanalytics2'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_4a143cadc412486aaa05a737e5bb2c40 }}
        images: 'ritextanalytics2.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_e4d7d1e4cdf64e378295a6c19aa12faa }}/rianalytics:${{ github.sha }}'